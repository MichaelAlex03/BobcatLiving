import React, { FC } from 'react'

interface ReviewProps{
    title: String,
    date: Number,
    content: String
}

export const ReviewCard: FC<ReviewProps> = (props): JSX.Element => {
  return (
    <div></div>
  )
}

export default ReviewCard
